# Web Forms Replacement Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace Excel upload with an 11-step wizard using Flask + htmx + Alpine.js web forms, generating FHIR Shorthand and JSON from in-memory pandas DataFrames.

**Architecture:** Flask session stores a dict mirroring the Excel sheet structure. Each wizard step is a GET/POST route. On POST, the form is validated (server-side via validator.py), data is stored in the session dict, and htmx redirects to the next step. The sidebar partial is re-rendered via htmx OOB swap. A new `create_from_session()` function in `functions.py` builds DataFrames from the session dict and runs the existing two-pass Jinja2 → sushi pipeline.

**Tech Stack:** Flask 3.x, Jinja2, htmx 1.x (CDN), Alpine.js 3.x (CDN), pandas, openpyxl (for lookup data only), sushi (Node.js subprocess)

---
### Task 0: Project Setup

**Files:**
- Modify: `requirements.txt`

- [ ] **Step 1: Add session dependency to requirements.txt**

```python
pandas
openpyxl==3.1.0
gunicorn
flask
```
Change to:
```python
pandas
openpyxl==3.1.0
gunicorn
flask
flask-session
cachelib
```

- [ ] **Step 2: Install new dependencies**

```bash
pip install flask-session cachelib
```

- [ ] **Step 3: Add session config to app factory (`epi_creator/__init__.py`)**

Add after `from flask import Flask`:
```python
from flask import Flask
from flask_session import Session
```

Add after `app = Flask(__name__)`:
```python
app.config["SECRET_KEY"] = os.urandom(24)
app.config["SESSION_TYPE"] = "filesystem"
app.config["SESSION_FILE_DIR"] = os.path.join(
    os.path.dirname(os.path.dirname(__file__)), "flask_session"
)
os.makedirs(app.config["SESSION_FILE_DIR"], exist_ok=True)
Session(app)
```

- [ ] **Step 4: Add .gitignore entry for session files**

Add to `.gitignore`:
```
flask_session/
.superpowers/
```

- [ ] **Step 5: Commit**
```bash
git add requirements.txt epi_creator/__init__.py .gitignore
git commit -m "chore: add flask-session for wizard session storage"
```

---
### Task 1: Refactor `functions.py` — Add `create_from_session()`

**Files:**
- Modify: `epi_creator/functions.py:161-283`

- [ ] **Step 1: Add `build_dataframes()` function that converts session dict to DataFrames**

Add after line 136 (after `create_env`), before `quality_checks`:

```python
def build_dataframes(session_data):
    dataframes = {}
    for sheet in elements:
        rows = session_data.get(sheet, [])
        if not rows:
            rows = [{}]
        df = pd.DataFrame(rows)
        df["id_hash"] = df["id"].apply(lambda x: uuid.uuid4()) if "id" in df.columns else [uuid.uuid4() for _ in range(len(df))]
        df["id"].fillna(df["id_hash"], inplace=True) if "id" in df.columns else df.__setitem__("id", df["id_hash"])
        df = df.astype(str)
        dataframes[sheet] = df
    return dataframes
```

- [ ] **Step 2: Add `create_from_session()` that replaces `create_from_template()`**

Add after `build_dataframes`:

```python
def create_from_session(env, session_data, TEMPLATE_FOLDER, OUTPUT_FOLDER, major_name):
    if TEMPLATE_FOLDER[-1] != "/":
        TEMPLATE_FOLDER += "/"
    if OUTPUT_FOLDER[-1] != "/":
        OUTPUT_FOLDER += "/"

    if not exists(OUTPUT_FOLDER):
        mkdir(OUTPUT_FOLDER)

    dataframes = build_dataframes(session_data)

    productname = "product"
    if "MedicinalProductDefinition" in dataframes and len(dataframes["MedicinalProductDefinition"]) > 0:
        productname = str(dataframes["MedicinalProductDefinition"].iloc[0].get("productname", "product"))

    data_dict = {
        "MajorName": major_name,
        "url": CANONICAL_URL,
        "productname": productname,
    }

    data = {
        "dictionary": data_dict,
        "turn": "1",
    }

    for sheet in elements:
        df = dataframes[sheet]
        t = env.get_template(sheet + ".fsh")
        data["data"] = df
        t.stream(data=data, **context).dump(OUTPUT_FOLDER + sheet + ".fsh")

    object_ids = {}
    for file in listdir(OUTPUT_FOLDER):
        with open(OUTPUT_FOLDER + file, "r") as f:
            lines = f.readlines()
            instances = []
            ids = []
            for line in lines:
                if "Instance: " in line:
                    val = line.replace("Instance: ", "").strip()
                    instances.append(val)
                    ids.append(val)
            object_ids[file.split(".")[0]] = [(i, j) for i, j in zip(instances, ids)]

    data["references"] = object_ids

    for sheet in elements:
        df = dataframes[sheet]
        t = env.get_template(sheet + ".fsh")
        data["data"] = df
        data["turn"] = "2"
        t.stream(data=data, **context).dump(OUTPUT_FOLDER + sheet + ".fsh")
```

- [ ] **Step 3: Add `generate_from_session()` to replace `process_file()` for the wizard flow**

Add after `create_from_session`:

```python
def generate_from_session(session_data, major_name):
    from os import makedirs as os_makedirs

    zip_folder = getcwd() + "/output/"
    if exists(zip_folder):
        shutil.rmtree(zip_folder)
    mkdir(zip_folder)

    real_output_folder = getcwd() + "/input/fsh/examples/"
    os_makedirs("input", exist_ok=True)
    os_makedirs("input/fsh", exist_ok=True)
    os_makedirs(real_output_folder, exist_ok=True)

    download_folder = "downloads/"
    if not exists(download_folder):
        mkdir(download_folder)

    env = create_env("templates/")
    create_from_session(env, session_data, "templates/", real_output_folder, major_name)

    result = subprocess.run(["sushi", "."], stdout=subprocess.PIPE)
    with open(download_folder + "/result.txt", "w") as f:
        f.write(result.stdout.decode("utf-8"))

    for json_file in listdir("fsh-generated/resources"):
        if json_file.startswith("Bundle"):
            shutil.move("fsh-generated/resources/" + json_file, zip_folder)

    zip_file_name = "epi_creator/" + major_name + "_results.zip"
    with ZipFile(zip_file_name, "w") as myzipfile:
        add_folder_to_zip(myzipfile, zip_folder, "json-files")
        add_folder_to_zip(myzipfile, real_output_folder, "fsh-files")
        add_folder_to_zip(myzipfile, download_folder, "result")

    return zip_file_name
```

- [ ] **Step 4: Commit**

```bash
git add epi_creator/functions.py
git commit -m "feat: add session-based FSH generation (no Excel dependency)"
```

---
### Task 2: Build `lookup.py` — Data Lookup Service

**Files:**
- Create: `epi_creator/lookup.py`

- [ ] **Step 1: Create `lookup.py`**

```python
import pandas as pd
import os

_lookups = {}

def load_lookups(excel_path="acmeDrug_1.xlsx"):
    """Load EXTRA and DATA_VAL sheets into module-level dicts. Call once at startup."""
    if not os.path.exists(excel_path):
        print(f"WARNING: lookup file {excel_path} not found, lookups will be empty")
        return

    extra = pd.read_excel(excel_path, sheet_name="EXTRA")
    data_val = pd.read_excel(excel_path, sheet_name="DATA_VAL")

    _lookups["doseForms"] = sorted(
        extra["100000072057_descr"].dropna().unique().tolist()
    )

    _lookups["routes"] = sorted(
        extra["100000073345_descr"].dropna().unique().tolist()
    )

    _lookups["unitPresentations"] = sorted(
        extra["100000000002_descr"].dropna().unique().tolist()
    )

    _lookups["countries"] = sorted(
        extra["sms_descr"].dropna().apply(
            lambda x: x if isinstance(x, str) and len(x) < 50 else None
        ).dropna().unique().tolist()
    )

    _lookups["orgTypes"] = [
        "Marketing Authorisation Holder",
        "Medicines Regulatory Authority",
        "Manufacturer",
        "Master File Holder",
        "Contact Location",
        "Manufacturer Batch Release",
        "Manufacturer API",
    ]

    _lookups["substances"] = sorted(
        extra["sms_descr"].dropna().unique().tolist()
    )

    _lookups["packagingTypes"] = ["Blister", "Bottle", "Carton", "Vial", "Ampoule",
                                    "Tube", "Sachet", "Syringe", "Bag", "Jar",
                                    "Pre-filled pen", "Pre-filled syringe",
                                    "Container", "Box", "Pouch", "Strip"]
    _lookups["packagingMaterials"] = ["PVC", "Aluminium", "Glass", "HDPE", "LDPE",
                                        "PET", "Paper", "Cardboard", "PVC/PVDC",
                                        "Aluminium/PVC", "Polypropylene", "Polystyrene"]

    _lookups["languages"] = [
        {"code": "en", "name": "English"},
        {"code": "pt", "name": "Portuguese"},
        {"code": "es", "name": "Spanish"},
        {"code": "fr", "name": "French"},
        {"code": "de", "name": "German"},
        {"code": "it", "name": "Italian"},
        {"code": "nl", "name": "Dutch"},
        {"code": "el", "name": "Greek"},
        {"code": "sv", "name": "Swedish"},
        {"code": "da", "name": "Danish"},
        {"code": "fi", "name": "Finnish"},
        {"code": "no", "name": "Norwegian"},
        {"code": "pl", "name": "Polish"},
        {"code": "cs", "name": "Czech"},
        {"code": "hu", "name": "Hungarian"},
        {"code": "ro", "name": "Romanian"},
        {"code": "bg", "name": "Bulgarian"},
        {"code": "hr", "name": "Croatian"},
        {"code": "lt", "name": "Lithuanian"},
        {"code": "lv", "name": "Latvian"},
        {"code": "et", "name": "Estonian"},
        {"code": "sk", "name": "Slovak"},
        {"code": "sl", "name": "Slovenian"},
        {"code": "mt", "name": "Maltese"},
        {"code": "ga", "name": "Irish"},
        {"code": "is", "name": "Icelandic"},
    ]

    _lookups["statusSupply"] = [
        {"id": "100000072078", "text": "No information available"},
        {"id": "100000072079", "text": "Never valid"},
        {"id": "100000072080", "text": "No supplied"},
        {"id": "100000072081", "text": "Valid"},
        {"id": "100000072082", "text": "Withdrawn"},
        {"id": "100000072083", "text": "Expired"},
        {"id": "100000072084", "text": "Suspended"},
        {"id": "100000072164", "text": "Not ongoing"},
        {"id": "100000072170", "text": "Ongoing but no marketing authorisation issued"},
        {"id": "200000019455", "text": "Renewed"},
        {"id": "200000019456", "text": "Revoked"},
        {"id": "200000019457", "text": "Refused"},
        {"id": "200000019458", "text": "Transferred"},
    ]

    _lookups["orgTypeIDs"] = {
        "Marketing Authorisation Holder": "220000000008",
        "Medicines Regulatory Authority": "220000000032",
        "Manufacturer": "220000000033",
        "Master File Holder": "220000000034",
        "Contact Location": "220000000035",
        "Manufacturer Batch Release": "220000000036",
        "Manufacturer API": "220000000037",
    }

    _lookups["ingredientRoles"] = [
        {"id": "100000072070", "name": "Active"},
        {"id": "100000072071", "name": "Adjuvant"},
        {"id": "100000072072", "name": "Excipient"},
        {"id": "100000072082", "name": "Solvent / Diluent"},
    ]

    _lookups["classificationItems"] = sorted(
        extra["100000155526_descr"].dropna().unique().tolist()
    )

    _lookups["clinicalUseTypes"] = ["indication", "contraindication", "interaction"]

    _lookups["clinicalUseConcepts"] = {}
    for ctype in _lookups["clinicalUseTypes"]:
        _lookups["clinicalUseConcepts"][ctype] = []

    _lookups["doseFormIDLookup"] = {}
    for _, row in extra.iterrows():
        code = row.get("100000072057")
        desc = row.get("100000072057_descr")
        if pd.notna(code) and pd.notna(desc):
            _lookups["doseFormIDLookup"][str(desc)] = str(int(code))

    _lookups["routeIDLookup"] = {}
    for _, row in extra.iterrows():
        code = row.get("100000073345")
        desc = row.get("100000073345_descr")
        if pd.notna(code) and pd.notna(desc):
            _lookups["routeIDLookup"][str(desc)] = str(int(code))

    _lookups["unitPresentationIDLookup"] = {}
    for _, row in extra.iterrows():
        code = row.get("100000000002")
        desc = row.get("100000000002_descr")
        if pd.notna(code) and pd.notna(desc):
            _lookups["unitPresentationIDLookup"][str(desc)] = str(int(code))

    print(f"lookup.py: Loaded {len(_lookups)} lookup categories")

def get_lookups():
    return _lookups

def get_lookup(category):
    return _lookups.get(category, [])

def get_category_id(category, description):
    lookup_map = {
        "doseForm": "doseFormIDLookup",
        "route": "routeIDLookup",
        "unitPresentation": "unitPresentationIDLookup",
    }
    key = lookup_map.get(category)
    if key and key in _lookups:
        return _lookups[key].get(description, "")
    return ""
```

- [ ] **Step 2: Wire `load_lookups()` into app startup**

In `epi_creator/__init__.py`, add after `from .views import gh_epi_creator`:

```python
from . import lookup as lookup_module
lookup_module.load_lookups()
```

- [ ] **Step 3: Commit**

```bash
git add epi_creator/lookup.py epi_creator/__init__.py
git commit -m "feat: add lookup data service from EXTRA/DATA_VAL sheets"
```

---
### Task 3: Update `validator.py` for Dict-Based Validation

**Files:**
- Modify: `epi_creator/validator.py`

- [ ] **Step 1: Replace file contents — rewrite for dict-based row validation**

Replace the entire file with:

```python
def validate_row(row, sheet):
    """Validate a single row dict. Returns list of error messages (empty = valid)."""
    errors = []

    def check_spaces(value, field_label):
        if value and isinstance(value, str) and " " in value:
            errors.append(f"{field_label} cannot contain spaces")

    def check_numeric(value, field_label):
        if value and isinstance(value, str) and value.strip():
            try:
                float(value)
            except ValueError:
                errors.append(f"{field_label} must be a number")
        elif value is None or (isinstance(value, str) and value.strip() == ""):
            pass
        else:
            try:
                float(value)
            except (ValueError, TypeError):
                errors.append(f"{field_label} must be a number")

    def check_no_newline(value, field_label):
        if value and isinstance(value, str) and "\n" in value:
            errors.append(f"{field_label} cannot contain newlines")

    def check_required(value, field_label):
        if not value or (isinstance(value, str) and value.strip() == ""):
            errors.append(f"{field_label} is required")

    # --- AdministrableProductDefinition ---
    if sheet == "AdministrableProductDefinition":
        check_spaces(row.get("status"), "Status")
        check_numeric(row.get("unit_presentationID"), "Unit Presentation ID")
        check_numeric(row.get("routeID"), "Route ID")
        check_numeric(row.get("doseFormID"), "Dose Form ID")
        check_no_newline(row.get("name"), "Name")

    # --- Ingredient ---
    elif sheet == "Ingredient":
        check_spaces(row.get("identifier"), "Identifier")
        check_spaces(row.get("StrengthBasis"), "Strength Basis")
        check_numeric(row.get("quantity"), "Quantity")
        check_numeric(row.get("roleID"), "Role ID")
        check_no_newline(row.get("name"), "Name")
        role = (row.get("role") or "").lower()
        if role in ("active", "ativo"):
            if not row.get("StrengthBasis") or str(row.get("StrengthBasis")).strip() == "":
                errors.append("Strength Basis is required for active ingredients")
            if not row.get("quantity") or str(row.get("quantity")).strip() == "":
                errors.append("Quantity is required for active ingredients")

    # --- ManufacturedItemDefinition ---
    elif sheet == "ManufacturedItemDefinition":
        check_spaces(row.get("status"), "Status")
        check_numeric(row.get("unit_presentationID"), "Unit Presentation ID")
        check_numeric(row.get("doseFormID"), "Dose Form ID")
        check_no_newline(row.get("name"), "Name")
        check_spaces(row.get("identifier"), "Identifier")

    # --- MedicinalProductDefinition ---
    elif sheet == "MedicinalProductDefinition":
        check_no_newline(row.get("productname"), "Product Name")
        check_spaces(row.get("status"), "Status")
        check_spaces(row.get("countryCode"), "Country Code")
        check_numeric(row.get("statusSuplyID"), "Status Supply ID")
        check_no_newline(row.get("inventedNamePart"), "Invented Name Part")
        check_no_newline(row.get("ScientificNamePart"), "Scientific Name Part")
        check_no_newline(row.get("StrengthPart"), "Strength Part")
        check_no_newline(row.get("PharmaceuticalDosePart"), "Pharmaceutical Dose Part")

    # --- Organization ---
    elif sheet == "Organization":
        check_spaces(row.get("identifier"), "Identifier")
        check_numeric(row.get("address_postalCode"), "Postal Code")
        check_numeric(row.get("typeID"), "Type ID")
        check_no_newline(row.get("name"), "Name")

    # --- PackagedProductDefinition ---
    elif sheet == "PackagedProductDefinition":
        check_spaces(row.get("identifier"), "Identifier")
        check_numeric(row.get("inside_packaging_typeID"), "Inside Packaging Type ID")
        check_numeric(row.get("inside_packaging_quantity"), "Inside Packaging Quantity")
        check_numeric(row.get("packaging_quantity"), "Packaging Quantity")
        check_numeric(row.get("Packaging_typeID"), "Packaging Type ID")
        check_numeric(row.get("packaging_materialID"), "Packaging Material ID")
        check_numeric(row.get("typeID"), "Type ID")
        check_no_newline(row.get("name"), "Name")

    # --- Substance ---
    elif sheet == "Substance":
        check_no_newline(row.get("name"), "Name")
        check_spaces(row.get("identifier"), "Identifier")

    # --- ClinicalUseDefinition ---
    elif sheet == "ClinicalUseDefinition":
        check_no_newline(row.get("name"), "Name")
        check_spaces(row.get("identifier"), "Identifier")
        check_numeric(row.get("conceptID"), "Concept ID")

    # --- Composition ---
    elif sheet == "Composition":
        check_no_newline(row.get("name"), "Name")
        if not row.get("language") or str(row.get("language")).strip() == "":
            errors.append("Language is required")

    # --- RegulatedAuthorization ---
    elif sheet == "RegulatedAuthorization":
        check_spaces(row.get("identifier"), "Identifier")
        check_numeric(row.get("typeID"), "Type ID")
        check_numeric(row.get("regionID"), "Region ID") if row.get("regionID") and str(row.get("regionID")).strip() != "" else None

    # --- Bundle ---
    elif sheet == "Bundle":
        if not row.get("language") or str(row.get("language")).strip() == "":
            errors.append("Language is required")

    return errors


def validate_sheet_data(rows, sheet_name):
    """Validate all rows for a sheet. Returns list of (row_index, errors) tuples."""
    all_errors = []
    for idx, row in enumerate(rows):
        errs = validate_row(row, sheet_name)
        if errs:
            all_errors.append((idx, errs))
    return all_errors


def validate_pre_generation(session_data):
    """Run checks before generation. Returns list of error messages."""
    errors = []

    required_sheets = [
        "MedicinalProductDefinition",
        "Organization",
        "Ingredient",
        "AdministrableProductDefinition",
        "ManufacturedItemDefinition",
        "Bundle",
    ]
    for sheet in required_sheets:
        if not session_data.get(sheet) or len(session_data[sheet]) == 0:
            errors.append(f"{sheet} is required. Please fill in the form.")

    singles = ["MedicinalProductDefinition", "ManufacturedItemDefinition", "AdministrableProductDefinition"]
    for sheet in singles:
        if sheet in session_data and len(session_data[sheet]) > 1:
            errors.append(f"{sheet} must have exactly one entry. Found {len(session_data[sheet])}.")

    if "Ingredient" in session_data:
        has_active = any(
            (r.get("role") or "").lower() in ("active", "ativo")
            for r in session_data["Ingredient"]
        )
        if not has_active:
            errors.append("At least one active ingredient is required.")

    orgs = session_data.get("Organization", [])
    has_mah = any(
        (r.get("type") or "").lower() == "marketing authorisation holder"
        for r in orgs
    )
    if not has_mah:
        errors.append("At least one Marketing Authorisation Holder organization is required.")

    return errors
```

- [ ] **Step 2: Update imports in `epi_creator/__init__.py`** (no changes needed — validator import already exists in functions.py)

- [ ] **Step 3: Commit**

```bash
git add epi_creator/validator.py
git commit -m "feat: rewrite validator for dict-based row validation with pre-gen checks"
```

---
### Task 4: Build Wizard Base Template + Sidebar + CSS

**Files:**
- Create: `epi_creator/templates/wizard/base.html`
- Create: `epi_creator/templates/wizard/_sidebar.html`
- Modify: `epi_creator/static/css/main.css`

- [ ] **Step 1: Create `epi_creator/templates/wizard/base.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ePI Creator — {{ step_title }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/main.css') }}">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-b5kHyXgcpbZJO/tY9Ul7kGkf1S0CWuKcCD38l8YkeH8z8QjE0GmW1gYU5S9FOnJ0"
            crossorigin="anonymous"></script>
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.14.1/dist/cdn.min.js"></script>
    <style>
        .wizard-container {
            display: flex;
            min-height: 100vh;
        }
        .wizard-sidebar {
            width: 280px;
            background: #f8f9fa;
            border-right: 1px solid #dee2e6;
            padding: 20px 15px;
            flex-shrink: 0;
        }
        .wizard-sidebar .step {
            display: flex;
            align-items: center;
            padding: 10px 12px;
            margin-bottom: 4px;
            border-radius: 6px;
            font-size: 14px;
            text-decoration: none;
            color: #6c757d;
            transition: background 0.15s;
        }
        .wizard-sidebar .step.active {
            background: #0d6efd;
            color: #fff;
            font-weight: 600;
        }
        .wizard-sidebar .step.completed {
            color: #198754;
            cursor: pointer;
        }
        .wizard-sidebar .step.completed:hover {
            background: #e9ecef;
        }
        .wizard-sidebar .step.pending {
            color: #adb5bd;
            pointer-events: none;
        }
        .wizard-sidebar .step-icon {
            width: 24px;
            height: 24px;
            border-radius: 50%;
            background: #dee2e6;
            color: #6c757d;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: 700;
            margin-right: 10px;
            flex-shrink: 0;
        }
        .wizard-sidebar .step.active .step-icon {
            background: #fff;
            color: #0d6efd;
        }
        .wizard-sidebar .step.completed .step-icon {
            background: #198754;
            color: #fff;
        }
        .wizard-sidebar .generate-btn {
            margin-top: 20px;
        }
        .wizard-main {
            flex: 1;
            padding: 30px 40px;
            overflow-y: auto;
        }
        .wizard-main h2 {
            margin-bottom: 6px;
        }
        .wizard-main .section-description {
            color: #6c757d;
            margin-bottom: 24px;
        }
        .btn-nav {
            min-width: 100px;
        }
        .error-message {
            color: #dc3545;
            font-size: 13px;
            margin-top: 4px;
        }
        .field-error {
            border-color: #dc3545 !important;
        }
        .autocomplete-wrapper {
            position: relative;
        }
        .autocomplete-dropdown {
            position: absolute;
            top: 100%;
            left: 0;
            right: 0;
            z-index: 1000;
            background: #fff;
            border: 1px solid #ced4da;
            border-top: none;
            max-height: 200px;
            overflow-y: auto;
            border-radius: 0 0 6px 6px;
        }
        .autocomplete-dropdown .item {
            padding: 6px 12px;
            cursor: pointer;
            font-size: 14px;
        }
        .autocomplete-dropdown .item:hover {
            background: #e9ecef;
        }
        .pipe-group {
            margin-bottom: 6px;
        }
        .pipe-group .input-group {
            margin-bottom: 4px;
        }
        .card-entry {
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 16px;
            margin-bottom: 12px;
        }
        .card-entry-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
        }
    </style>
</head>
<body hx-headers='{"X-CSRFToken": "{{ csrf_token() if csrf_token else "" }}"}'>
<div class="wizard-container">
    {% include 'wizard/_sidebar.html' %}
    <div class="wizard-main">
        {% block content %}{% endblock %}
    </div>
</div>
</body>
</html>
```

- [ ] **Step 2: Create `epi_creator/templates/wizard/_sidebar.html`**

```html
<div class="wizard-sidebar" id="wizard-sidebar" hx-swap-oob="true">
    <h5 style="margin-bottom:16px;font-weight:700">ePI Creator</h5>

    {% set all_steps = [
        ('organization', '1. Organization'),
        ('medicinal-product', '2. Medicinal Product'),
        ('substance', '3. Substance'),
        ('ingredient', '4. Ingredient'),
        ('regulated-auth', '5. Regulated Auth'),
        ('manufactured-item', '6. Manufactured Item'),
        ('administrable-product', '7. Administrable Product'),
        ('packaged-product', '8. Packaged Product'),
        ('clinical-use', '9. Clinical Use'),
        ('composition', '10. Composition'),
        ('bundle', '11. Bundle'),
    ] %}

    {% set current_index = steps.index(current_step) if current_step in steps else 0 %}

    {% for step_key, step_label in all_steps %}
        {% set idx = loop.index0 %}
        {% if idx < current_index %}
            {% set state = 'completed' %}
            {% set icon = '&#10003;' %}
        {% elif idx == current_index %}
            {% set state = 'active' %}
            {% set icon = loop.index|string %}
        {% else %}
            {% set state = 'pending' %}
            {% set icon = loop.index|string %}
        {% endif %}
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=step_key) }}"
           class="step {{ state }}">
            <span class="step-icon">{{ icon|safe }}</span>
            {{ step_label }}
        </a>
    {% endfor %}

    <button id="generate-btn"
            class="btn btn-success generate-btn w-100"
            hx-post="{{ url_for('gh_epi_creator.wizard_generate') }}"
            hx-target="#generate-result"
            hx-indicator="#generate-spinner"
            {% if not session.get('data') or session['data']|length < 11 %}disabled{% endif %}>
        Generate FHIR Package
    </button>
    <div id="generate-spinner" class="htmx-indicator text-center mt-2" style="font-size:13px;color:#6c757d">
        Processing...
    </div>
    <div id="generate-result" class="mt-2"></div>

    <!-- preserve step tracking -->
    <script>
        if (typeof window.__wizardCurrentIndex === 'undefined') {
            window.__wizardCurrentIndex = 0;
        }
        window.__wizardCurrentIndex = {{ current_index }};
    </script>
</div>
```

- [ ] **Step 3: Update `epi_creator/static/css/main.css`**

Add to the existing CSS file:
```css
.htmx-indicator { display: none; }
.htmx-request .htmx-indicator { display: inline-block; }
.htmx-request#generate-btn { opacity: 0.7; pointer-events: none; }
```

- [ ] **Step 4: Commit**

```bash
git add epi_creator/templates/wizard/base.html epi_creator/templates/wizard/_sidebar.html epi_creator/static/css/main.css
git commit -m "feat: add wizard base layout with sidebar and session tracking"
```

---
### Task 5: Create Client-Side JS (Autocomplete + Validation)

**Files:**
- Create: `epi_creator/static/js/autocomplete.js`

- [ ] **Step 1: Create `epi_creator/static/js/autocomplete.js`**

```javascript
document.addEventListener('alpine:init', () => {
    Alpine.data('autocomplete', (category, idField = '') => ({
        query: '',
        items: [],
        open: false,
        selectedLabel: '',
        selectedId: '',

        init() {
            this.selectedId = this.$el.querySelector('input[type="hidden"]')?.value || '';
        },

        async search() {
            if (this.query.length < 1) { this.items = []; this.open = false; return; }
            try {
                const resp = await fetch(`/gh-epi-creator/api/lookup/${category}?q=${encodeURIComponent(this.query)}`);
                this.items = await resp.json();
                this.open = this.items.length > 0;
            } catch (e) {
                this.items = [];
                this.open = false;
            }
        },

        select(item) {
            if (typeof item === 'object') {
                this.selectedLabel = item.label || item.name || item;
                this.selectedId = item.id || item.code || '';
            } else {
                this.selectedLabel = item;
                this.selectedId = item;
            }
            this.query = '';
            this.open = false;
            if (idField) {
                const hidden = this.$el.querySelector(`input[name="${idField}"]`);
                if (hidden) hidden.value = this.selectedId;
            }
        },

        clear() {
            this.selectedLabel = '';
            this.selectedId = '';
            this.query = '';
        }
    }));
});
```

- [ ] **Step 2: Commit**

```bash
git add epi_creator/static/js/autocomplete.js
git commit -m "feat: add Alpine.js autocomplete component for lookup data"
```

---
### Task 6: Update `views.py` — Add Wizard Routes + Lookup API

**Files:**
- Modify: `epi_creator/views.py`

- [ ] **Step 1: Rewrite `views.py`**

Replace the entire file with:

```python
import os

from flask import (
    Blueprint,
    jsonify,
    redirect,
    render_template,
    request,
    send_from_directory,
    session,
    url_for,
)
from flask import current_app as app

from epi_creator.functions import generate_from_session
from epi_creator.validator import validate_row, validate_sheet_data, validate_pre_generation
from epi_creator.lookup import get_lookup, get_category_id

gh_epi_creator = Blueprint("gh_epi_creator", __name__)

STEPS = [
    "organization",
    "medicinal-product",
    "substance",
    "ingredient",
    "regulated-auth",
    "manufactured-item",
    "administrable-product",
    "packaged-product",
    "clinical-use",
    "composition",
    "bundle",
]

SHEET_NAMES = {
    "organization": "Organization",
    "medicinal-product": "MedicinalProductDefinition",
    "substance": "Substance",
    "ingredient": "Ingredient",
    "regulated-auth": "RegulatedAuthorization",
    "manufactured-item": "ManufacturedItemDefinition",
    "administrable-product": "AdministrableProductDefinition",
    "packaged-product": "PackagedProductDefinition",
    "clinical-use": "ClinicalUseDefinition",
    "composition": "Composition",
    "bundle": "Bundle",
}


@gh_epi_creator.before_request
def log_request_info():
    app.logger.info(f"Request: {request.method} {request.path}")


@gh_epi_creator.route("/", methods=["GET"])
def hello():
    return render_template("index.html")


@gh_epi_creator.route("/faq", methods=["GET"])
def faq():
    return render_template("faq.html")


@gh_epi_creator.route("/wizard/new", methods=["POST"])
def wizard_new():
    session.clear()
    session["data"] = {}
    return redirect(url_for("gh_epi_creator.wizard_step", step=STEPS[0]))


@gh_epi_creator.route("/wizard/<step>", methods=["GET"])
def wizard_step(step):
    if "data" not in session:
        session["data"] = {}
    data = session["data"]
    sheet_name = SHEET_NAMES.get(step)
    rows = data.get(sheet_name, [])

    return render_template(
        "wizard/base.html",
        step=step,
        sheet_name=sheet_name,
        rows=rows,
        steps=STEPS,
        current_step=step,
        step_title=sheet_name.replace("Definition", "").replace("Definition", ""),
        is_single=sheet_name in ("MedicinalProductDefinition", "ManufacturedItemDefinition", "AdministrableProductDefinition"),
    )


@gh_epi_creator.route("/wizard/<step>", methods=["POST"])
def wizard_submit(step):
    if "data" not in session:
        session["data"] = {}
    sheet_name = SHEET_NAMES.get(step)

    form_data = request.form.to_dict(flat=False)
    rows = parse_form_rows(form_data, sheet_name)

    validation_errors = validate_sheet_data(rows, sheet_name)

    if validation_errors:
        return render_template(
            "wizard/base.html",
            step=step,
            sheet_name=sheet_name,
            rows=rows,
            steps=STEPS,
            current_step=step,
            step_title=sheet_name.replace("Definition", ""),
            errors=validation_errors,
            is_single=sheet_name in ("MedicinalProductDefinition", "ManufacturedItemDefinition", "AdministrableProductDefinition"),
        )

    session["data"][sheet_name] = rows
    session.modified = True

    current_idx = STEPS.index(step)
    if current_idx < len(STEPS) - 1:
        next_step = STEPS[current_idx + 1]
        return render_template(
            "wizard/base.html",
            step=next_step,
            sheet_name=SHEET_NAMES[next_step],
            rows=session["data"].get(SHEET_NAMES[next_step], []),
            steps=STEPS,
            current_step=next_step,
            step_title=SHEET_NAMES[next_step].replace("Definition", ""),
            is_single=SHEET_NAMES[next_step] in ("MedicinalProductDefinition", "ManufacturedItemDefinition", "AdministrableProductDefinition"),
        )
    else:
        return render_template(
            "wizard/base.html",
            step="bundle",
            sheet_name="Bundle",
            rows=session["data"].get("Bundle", []),
            steps=STEPS,
            current_step="bundle",
            step_title="Bundle",
            is_single=False,
        )


@gh_epi_creator.route("/wizard/<step>/prev", methods=["GET"])
def wizard_prev(step):
    current_idx = STEPS.index(step)
    if current_idx > 0:
        return redirect(url_for("gh_epi_creator.wizard_step", step=STEPS[current_idx - 1]))
    return redirect(url_for("gh_epi_creator.wizard_step", step=STEPS[0]))


@gh_epi_creator.route("/wizard/generate", methods=["POST"])
def wizard_generate():
    if "data" not in session or not session["data"]:
        return jsonify({"error": "No data to generate. Please fill in the forms first."})

    pre_gen_errors = validate_pre_generation(session["data"])
    if pre_gen_errors:
        return "<div class='alert alert-danger'><ul>" + "".join(f"<li>{e}</li>" for e in pre_gen_errors) + "</ul></div>"

    productname = "epi-product"
    mp = session["data"].get("MedicinalProductDefinition", [{}])
    if mp and mp[0].get("productname"):
        productname = mp[0]["productname"].replace(" ", "_")

    try:
        zip_path = generate_from_session(session["data"], productname)
        download_url = url_for("gh_epi_creator.wizard_download", filename=os.path.basename(zip_path))
        return f"<a href='{download_url}' class='btn btn-primary mt-2' download>Download Results (ZIP)</a>"
    except Exception as e:
        return f"<div class='alert alert-danger'>Generation failed: {str(e)}</div>"


@gh_epi_creator.route("/wizard/download")
def wizard_download():
    filename = request.args.get("filename", "")
    return send_from_directory(
        directory=os.path.dirname(filename) if os.path.dirname(filename) else ".",
        path=os.path.basename(filename),
        as_attachment=True,
    )


@gh_epi_creator.route("/api/lookup/<category>", methods=["GET"])
def api_lookup(category):
    q = request.args.get("q", "").lower()
    items = get_lookup(category)

    if q:
        items = [i for i in items if q in str(i).lower()]

    result = []
    for item in items[:50]:
        if isinstance(item, dict):
            result.append(item)
        else:
            result.append({"label": str(item), "value": str(item)})
    return jsonify(result)


def parse_form_rows(form_data, sheet_name):
    """Parse flat form data into list of row dicts."""
    rows = []

    row_counts = [int(v[0]) for k, v in form_data.items() if k.startswith("row_count_")]
    max_rows = max(row_counts) if row_counts else 1

    for i in range(max_rows):
        row = {}
        for key, values in form_data.items():
            if key.startswith("row_count_"):
                continue
            suffix = f"_{i}"
            if key.endswith(suffix):
                col_name = key[:-len(suffix)]
                row[col_name] = values[0] if values else ""
        if row:
            rows.append(row)

    if not rows:
        prefix_found = any(k.startswith(sheet_name + "_") or not k.startswith("row_count_") for k in form_data)
        if form_data and not prefix_found:
            pass

    return rows
```

- [ ] **Step 2: Verify imports resolve**

```bash
python3 -c "from epi_creator.views import gh_epi_creator; print('OK')"
```

- [ ] **Step 3: Commit**

```bash
git add epi_creator/views.py
git commit -m "feat: add wizard routes, lookup API, and session-based form handling"
```

---
### Task 7: Create Form Templates (11 forms)

**Files:**
- Create: `epi_creator/templates/wizard/organization.html` (multi, table-based)
- Create: `epi_creator/templates/wizard/medicinal_product.html` (single, form)
- Create: `epi_creator/templates/wizard/substance.html` (multi, table-based)
- Create: `epi_creator/templates/wizard/ingredient.html` (multi, table-based)
- Create: `epi_creator/templates/wizard/regulated_auth.html` (multi, table-based)
- Create: `epi_creator/templates/wizard/manufactured_item.html` (single, form)
- Create: `epi_creator/templates/wizard/administrable_product.html` (single, form)
- Create: `epi_creator/templates/wizard/packaged_product.html` (multi, table with sub-rows)
- Create: `epi_creator/templates/wizard/clinical_use.html` (multi, card-based)
- Create: `epi_creator/templates/wizard/composition.html` (multi, card-based)
- Create: `epi_creator/templates/wizard/bundle.html` (simple)

- [ ] **Step 1: Create `organization.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Organization</h2>
<p class="section-description">Define the organizations involved (MAH, manufacturer, regulator, etc.). Add one row per organization.</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="{{ rows|length if rows else 1 }}">

    <div id="org-rows">
        {% set row_list = rows if rows and rows|length > 0 else [{}] %}
        {% for row in row_list %}
        <div class="card-entry mb-3" id="org-row-{{ loop.index0 }}">
            <div class="card-entry-header">
                <strong>Organization #{{ loop.index }}</strong>
                <button type="button" class="btn btn-sm btn-outline-danger"
                        onclick="this.closest('.card-entry').remove(); updateRowCount();"
                        {% if row_list|length <= 1 and loop.index0 == 0 %}style="display:none"{% endif %}>
                    Remove
                </button>
            </div>
            <div class="row g-2">
                <div class="col-md-4">
                    <label class="form-label">Name *</label>
                    <input class="form-control {{ 'field-error' if errors and errors|selectattr('0','eq',loop.index0)|list|length > 0 }}"
                           name="name_{{ loop.index0 }}" value="{{ row.get('name','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Identifier</label>
                    <input class="form-control" name="identifier_{{ loop.index0 }}" value="{{ row.get('identifier','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Organization Type *</label>
                    <select class="form-select" name="type_{{ loop.index0 }}">
                        <option value="">-- Select --</option>
                        {% set types = ['Marketing Authorisation Holder','Medicines Regulatory Authority','Manufacturer','Master File Holder','Contact Location','Manufacturer Batch Release','Manufacturer API'] %}
                        {% for t in types %}
                        <option value="{{ t }}" {% if row.get('type','') == t %}selected{% endif %}>{{ t }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label">Type ID</label>
                    <input class="form-control" name="typeID_{{ loop.index0 }}" value="{{ row.get('typeID','') }}" placeholder="auto-filled">
                </div>
                <div class="col-md-6">
                    <label class="form-label">Address Line</label>
                    <input class="form-control" name="address_line_{{ loop.index0 }}" value="{{ row.get('address_line','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">City</label>
                    <input class="form-control" name="address_city_{{ loop.index0 }}" value="{{ row.get('address_city','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Country</label>
                    <input class="form-control" name="address_country_{{ loop.index0 }}" value="{{ row.get('address_country','') }}">
                </div>
                <div class="col-md-1">
                    <label class="form-label">Postal Code</label>
                    <input class="form-control" name="address_postalCode_{{ loop.index0 }}" value="{{ row.get('address_postalCode','') }}">
                </div>
                <input type="hidden" name="id_{{ loop.index0 }}" value="{{ row.get('id','') }}">
            </div>
            {% if errors %}
                {% for e_idx, e_msgs in errors %}
                    {% if e_idx == loop.index0 %}
                        {% for msg in e_msgs %}
                            <div class="error-message">{{ msg }}</div>
                        {% endfor %}
                    {% endif %}
                {% endfor %}
            {% endif %}
        </div>
        {% endfor %}
    </div>

    <button type="button" class="btn btn-sm btn-outline-secondary mb-3" onclick="addOrgRow()">+ Add Organization</button>

    <div class="d-flex justify-content-between mt-3">
        {% if steps.index(step) > 0 %}
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        {% else %}
        <span></span>
        {% endif %}
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>

<script>
let rowCount = {{ row_list|length if row_list else 1 }};
function addOrgRow() {
    const container = document.getElementById('org-rows');
    const div = document.createElement('div');
    div.className = 'card-entry mb-3';
    div.id = 'org-row-' + rowCount;
    div.innerHTML = `
        <div class="card-entry-header">
            <strong>Organization #${rowCount + 1}</strong>
            <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
        </div>
        <div class="row g-2">
            <div class="col-md-4"><label class="form-label">Name *</label><input class="form-control" name="name_${rowCount}"></div>
            <div class="col-md-3"><label class="form-label">Identifier</label><input class="form-control" name="identifier_${rowCount}"></div>
            <div class="col-md-3"><label class="form-label">Organization Type *</label>
                <select class="form-select" name="type_${rowCount}">
                    <option value="">-- Select --</option>
                    <option>Marketing Authorisation Holder</option>
                    <option>Medicines Regulatory Authority</option>
                    <option>Manufacturer</option>
                    <option>Master File Holder</option>
                    <option>Contact Location</option>
                    <option>Manufacturer Batch Release</option>
                    <option>Manufacturer API</option>
                </select></div>
            <div class="col-md-2"><label class="form-label">Type ID</label><input class="form-control" name="typeID_${rowCount}" placeholder="auto-filled"></div>
            <div class="col-md-6"><label class="form-label">Address Line</label><input class="form-control" name="address_line_${rowCount}"></div>
            <div class="col-md-3"><label class="form-label">City</label><input class="form-control" name="address_city_${rowCount}"></div>
            <div class="col-md-2"><label class="form-label">Country</label><input class="form-control" name="address_country_${rowCount}"></div>
            <div class="col-md-1"><label class="form-label">Postal Code</label><input class="form-control" name="address_postalCode_${rowCount}"></div>
            <input type="hidden" name="id_${rowCount}">
        </div>`;
    container.appendChild(div);
    rowCount++;
    updateRowCount();
}
function updateRowCount() {
    const rows = document.querySelectorAll('#org-rows .card-entry');
    document.querySelector('input[name="row_count"]').value = rows.length;
    rows.forEach((r, i) => {
        const oldId = r.id;
        r.id = 'org-row-' + i;
        r.querySelector('strong').textContent = 'Organization #' + (i + 1);
        r.querySelectorAll('input, select').forEach(el => {
            el.name = el.name.replace(/_\d+$/, '_' + i);
        });
    });
    rowCount = rows.length;
}
</script>
{% endblock %}
```

- [ ] **Step 2: Create `medicinal_product.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Medicinal Product Definition</h2>
<p class="section-description">Core details of the medicinal product. Exactly one entry.</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="1">

    {% set row = rows[0] if rows and rows|length > 0 else {} %}
    <div class="row g-2 mb-3">
        <div class="col-md-6">
            <label class="form-label">Product Name *</label>
            <input class="form-control {{ 'field-error' if errors else '' }}" name="productname_0" value="{{ row.get('productname','') }}">
        </div>
        <div class="col-md-6">
            <label class="form-label">Invented Name Part</label>
            <input class="form-control" name="inventedNamePart_0" value="{{ row.get('inventedNamePart','') }}">
        </div>
        <div class="col-md-6">
            <label class="form-label">Scientific Name Part</label>
            <input class="form-control" name="ScientificNamePart_0" value="{{ row.get('ScientificNamePart','') }}">
        </div>
        <div class="col-md-3">
            <label class="form-label">Strength Part</label>
            <input class="form-control" name="StrengthPart_0" value="{{ row.get('StrengthPart','') }}">
        </div>
        <div class="col-md-3">
            <label class="form-label">Pharmaceutical Dose Part</label>
            <input class="form-control" name="PharmaceuticalDosePart_0" value="{{ row.get('PharmaceuticalDosePart','') }}">
        </div>
    </div>
    <div class="row g-2 mb-3">
        <div class="col-md-4">
            <label class="form-label">Country</label>
            <input class="form-control" name="country_0" value="{{ row.get('country','') }}">
        </div>
        <div class="col-md-2">
            <label class="form-label">Country Code</label>
            <input class="form-control" name="countryCode_0" value="{{ row.get('countryCode','') }}" placeholder="e.g. EU">
        </div>
        <div class="col-md-3">
            <label class="form-label">Language</label>
            <input class="form-control" name="language_0" value="{{ row.get('language','') }}" placeholder="e.g. English">
        </div>
        <div class="col-md-3">
            <label class="form-label">Language ID</label>
            <input class="form-control" name="languageID_0" value="{{ row.get('languageID','') }}" placeholder="e.g. en">
        </div>
    </div>
    <div class="row g-2 mb-3">
        <div class="col-md-4">
            <label class="form-label">Status Supply</label>
            <input class="form-control" name="statusSuply_0" value="{{ row.get('statusSuply','') }}">
        </div>
        <div class="col-md-2">
            <label class="form-label">Status Supply ID</label>
            <input class="form-control" name="statusSuplyID_0" value="{{ row.get('statusSuplyID','') }}">
        </div>
        <div class="col-md-6">
            <label class="form-label">Indication (narrative text)</label>
            <textarea class="form-control" name="indication_0" rows="3">{{ row.get('indication','') }}</textarea>
        </div>
    </div>

    <!-- Pipe-delimited identifiers -->
    <div class="mb-3">
        <label class="form-label">Identifiers</label>
        <div id="identifiers-container">
            {% set id_systems = (row.get('identifier_system','') or '').split('|') %}
            {% set id_values = (row.get('identifier_value','') or '').split('|') %}
            {% for i in range([id_systems|length, id_values|length, 1]|max) %}
            <div class="input-group mb-1 pipe-group">
                <input class="form-control" name="identifier_system_0_p{{ i }}" value="{{ id_systems[i] if i < id_systems|length else '' }}" placeholder="System">
                <input class="form-control" name="identifier_value_0_p{{ i }}" value="{{ id_values[i] if i < id_values|length else '' }}" placeholder="Value">
                <button type="button" class="btn btn-outline-danger" onclick="this.closest('.pipe-group').remove()">x</button>
            </div>
            {% endfor %}
        </div>
        <button type="button" class="btn btn-sm btn-outline-secondary" onclick="addPipeField('identifiers')">+ Add Identifier</button>
    </div>

    <!-- Pipe-delimited classifications -->
    <div class="mb-3">
        <label class="form-label">ATC Classifications</label>
        <div id="classifications-container">
            {% set cls_ids = (row.get('classification_ids','') or '').split('|') %}
            {% set cls_texts = (row.get('classification_texts','') or '').split('|') %}
            {% for i in range([cls_ids|length, cls_texts|length, 1]|max) %}
            <div class="input-group mb-1 pipe-group">
                <input class="form-control" name="classification_ids_0_p{{ i }}" value="{{ cls_ids[i] if i < cls_ids|length else '' }}" placeholder="Code">
                <input class="form-control" name="classification_texts_0_p{{ i }}" value="{{ cls_texts[i] if i < cls_texts|length else '' }}" placeholder="Text">
                <button type="button" class="btn btn-outline-danger" onclick="this.closest('.pipe-group').remove()">x</button>
            </div>
            {% endfor %}
        </div>
        <button type="button" class="btn btn-sm btn-outline-secondary" onclick="addPipeField('classifications')">+ Add Classification</button>
    </div>

    <input type="hidden" name="id_0" value="{{ row.get('id','') }}">

    {% if errors %}
        {% for e_idx, e_msgs in errors %}
            {% for msg in e_msgs %}
                <div class="error-message">{{ msg }}</div>
            {% endfor %}
        {% endfor %}
    {% endif %}

    <div class="d-flex justify-content-between mt-4">
        {% if steps.index(step) > 0 %}
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        {% else %}
        <span></span>
        {% endif %}
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>

<script>
let pipeCounters = { identifiers: {{ [0, (row.get('identifier_system','') or '').split('|')|length - 1]|max }}, classifications: {{ [0, (row.get('classification_ids','') or '').split('|')|length - 1]|max }} };
function addPipeField(type) {
    pipeCounters[type] = (pipeCounters[type] || 0) + 1;
    const i = pipeCounters[type];
    const containerId = type === 'identifiers' ? 'identifiers-container' : 'classifications-container';
    const namePrefix = type === 'identifiers' ? 'identifier' : 'classification';
    const div = document.createElement('div');
    div.className = 'input-group mb-1 pipe-group';
    div.innerHTML = `
        <input class="form-control" name="${namePrefix}_system_0_p${i}" placeholder="System/Code">
        <input class="form-control" name="${namePrefix}_value_0_p${i}" placeholder="Value/Text">
        <button type="button" class="btn btn-outline-danger" onclick="this.closest('.pipe-group').remove()">x</button>`;
    document.getElementById(containerId).appendChild(div);
}
</script>
{% endblock %}
```

- [ ] **Step 3: Create `substance.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Substance</h2>
<p class="section-description">Define the active substance(s). Add one row per substance.</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="{{ rows|length if rows else 1 }}">

    <div id="substance-rows">
        {% set row_list = rows if rows and rows|length > 0 else [{}] %}
        {% for row in row_list %}
        <div class="card-entry mb-3">
            <div class="card-entry-header">
                <strong>Substance #{{ loop.index }}</strong>
                <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
            </div>
            <div class="row g-2">
                <div class="col-md-6">
                    <label class="form-label">Name *</label>
                    <input class="form-control" name="name_{{ loop.index0 }}" value="{{ row.get('name','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Identifier (GSRS)</label>
                    <input class="form-control" name="identifier_{{ loop.index0 }}" value="{{ row.get('identifier','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Version</label>
                    <input class="form-control" name="version_{{ loop.index0 }}" value="{{ row.get('version','') }}">
                </div>
                <div class="col-md-12">
                    <label class="form-label">Description</label>
                    <textarea class="form-control" name="description_{{ loop.index0 }}" rows="2">{{ row.get('description','') }}</textarea>
                </div>
                <div class="col-md-4">
                    <label class="form-label">Molecular Formula</label>
                    <input class="form-control" name="molecularFormula_{{ loop.index0 }}" value="{{ row.get('molecularFormula','') }}">
                </div>
                <div class="col-md-4">
                    <label class="form-label">Molecular Weight</label>
                    <input class="form-control" name="moleclularWeigth_{{ loop.index0 }}" value="{{ row.get('moleclularWeigth','') }}" placeholder="Pipe-delimited: 250|500">
                </div>
                <div class="col-md-4">
                    <label class="form-label">Weight Type</label>
                    <input class="form-control" name="moleclularWeigthType_{{ loop.index0 }}" value="{{ row.get('moleclularWeigthType','') }}" placeholder="Pipe-delimited">
                </div>
                <input type="hidden" name="id_{{ loop.index0 }}" value="{{ row.get('id','') }}">
            </div>
        </div>
        {% endfor %}
    </div>

    <button type="button" class="btn btn-sm btn-outline-secondary mb-3" onclick="addRow('substance');">+ Add Substance</button>

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>

<script>
let rowCounter = {{ row_list|length if row_list else 1 }};
function addRow(type) {
    const container = document.getElementById(type + '-rows');
    const div = document.createElement('div');
    div.className = 'card-entry mb-3';
    div.innerHTML = `
        <div class="card-entry-header">
            <strong>Substance #${rowCounter + 1}</strong>
            <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
        </div>
        <div class="row g-2">
            <div class="col-md-6"><label class="form-label">Name *</label><input class="form-control" name="name_${rowCounter}"></div>
            <div class="col-md-3"><label class="form-label">Identifier (GSRS)</label><input class="form-control" name="identifier_${rowCounter}"></div>
            <div class="col-md-3"><label class="form-label">Version</label><input class="form-control" name="version_${rowCounter}"></div>
            <div class="col-md-12"><label class="form-label">Description</label><textarea class="form-control" name="description_${rowCounter}" rows="2"></textarea></div>
            <div class="col-md-4"><label class="form-label">Molecular Formula</label><input class="form-control" name="molecularFormula_${rowCounter}"></div>
            <div class="col-md-4"><label class="form-label">Molecular Weight</label><input class="form-control" name="moleclularWeigth_${rowCounter}" placeholder="Pipe-delimited: 250|500"></div>
            <div class="col-md-4"><label class="form-label">Weight Type</label><input class="form-control" name="moleclularWeigthType_${rowCounter}" placeholder="Pipe-delimited"></div>
            <input type="hidden" name="id_${rowCounter}">
        </div>`;
    container.appendChild(div);
    rowCounter++;
    updateRowCount();
}
function updateRowCount() {
    const rows = document.querySelectorAll('#substance-rows .card-entry');
    document.querySelector('input[name="row_count"]').value = rows.length;
    rows.forEach((r, i) => {
        r.querySelector('strong').textContent = 'Substance #' + (i + 1);
        r.querySelectorAll('input, textarea').forEach(el => {
            el.name = el.name.replace(/_\d+$/, '_' + i);
        });
    });
    rowCounter = rows.length;
}
</script>
{% endblock %}
```

- [ ] **Step 4: Create `ingredient.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Ingredient</h2>
<p class="section-description">Define the ingredients (active + excipients). Add one row per ingredient.</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="{{ rows|length if rows else 1 }}">

    <div id="ingredient-rows">
        {% set row_list = rows if rows and rows|length > 0 else [{}] %}
        {% for row in row_list %}
        <div class="card-entry mb-3">
            <div class="card-entry-header">
                <strong>Ingredient #{{ loop.index }}</strong>
                <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
            </div>
            <div class="row g-2">
                <div class="col-md-4">
                    <label class="form-label">Name *</label>
                    <input class="form-control" name="name_{{ loop.index0 }}" value="{{ row.get('name','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Role</label>
                    <select class="form-select" name="role_{{ loop.index0 }}" onchange="handleRoleChange(this, {{ loop.index0 }})">
                        <option value="">-- Select --</option>
                        <option value="Active" {% if row.get('role','') == 'Active' %}selected{% endif %}>Active</option>
                        <option value="Excipient" {% if row.get('role','') == 'Excipient' %}selected{% endif %}>Excipient</option>
                        <option value="Adjuvant" {% if row.get('role','') == 'Adjuvant' %}selected{% endif %}>Adjuvant</option>
                        <option value="Solvent / Diluent" {% if row.get('role','') == 'Solvent / Diluent' %}selected{% endif %}>Solvent / Diluent</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label">Role ID</label>
                    <input class="form-control" name="roleID_{{ loop.index0 }}" value="{{ row.get('roleID','') }}" placeholder="e.g. 100000072070">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Identifier (GSRS)</label>
                    <input class="form-control" name="identifier_{{ loop.index0 }}" value="{{ row.get('identifier','') }}">
                </div>
            </div>
            <div class="row g-2 mt-1">
                <div class="col-md-3">
                    <label class="form-label">Strength Basis</label>
                    <input class="form-control strength-basis" name="StrengthBasis_{{ loop.index0 }}" value="{{ row.get('StrengthBasis','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Strength Basis Text</label>
                    <input class="form-control" name="StrengthBasisText_{{ loop.index0 }}" value="{{ row.get('StrengthBasisText','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Quantity</label>
                    <input class="form-control" name="quantity_{{ loop.index0 }}" value="{{ row.get('quantity','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Quantity Unit</label>
                    <input class="form-control" name="quantity unit_{{ loop.index0 }}" value="{{ row.get('quantity unit','') }}">
                </div>
                <input type="hidden" name="id_{{ loop.index0 }}" value="{{ row.get('id','') }}">
            </div>
            {% if errors %}
                {% for e_idx, e_msgs in errors %}
                    {% if e_idx == loop.index0 %}
                        {% for msg in e_msgs %}
                            <div class="error-message">{{ msg }}</div>
                        {% endfor %}
                    {% endif %}
                {% endfor %}
            {% endif %}
        </div>
        {% endfor %}
    </div>

    <button type="button" class="btn btn-sm btn-outline-secondary mb-3" onclick="addIngredientRow()">+ Add Ingredient</button>

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>

<script>
let ingRowCount = {{ row_list|length if row_list else 1 }};
function addIngredientRow() {
    const container = document.getElementById('ingredient-rows');
    const div = document.createElement('div');
    div.className = 'card-entry mb-3';
    div.innerHTML = `
        <div class="card-entry-header">
            <strong>Ingredient #${ingRowCount + 1}</strong>
            <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
        </div>
        <div class="row g-2">
            <div class="col-md-4"><label class="form-label">Name *</label><input class="form-control" name="name_${ingRowCount}"></div>
            <div class="col-md-3"><label class="form-label">Role</label>
                <select class="form-select" name="role_${ingRowCount}">
                    <option value="">-- Select --</option>
                    <option>Active</option><option>Excipient</option><option>Adjuvant</option><option>Solvent / Diluent</option>
                </select></div>
            <div class="col-md-2"><label class="form-label">Role ID</label><input class="form-control" name="roleID_${ingRowCount}" placeholder="e.g. 100000072070"></div>
            <div class="col-md-3"><label class="form-label">Identifier (GSRS)</label><input class="form-control" name="identifier_${ingRowCount}"></div>
        </div>
        <div class="row g-2 mt-1">
            <div class="col-md-3"><label class="form-label">Strength Basis</label><input class="form-control" name="StrengthBasis_${ingRowCount}"></div>
            <div class="col-md-3"><label class="form-label">Strength Basis Text</label><input class="form-control" name="StrengthBasisText_${ingRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Quantity</label><input class="form-control" name="quantity_${ingRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Quantity Unit</label><input class="form-control" name="quantity unit_${ingRowCount}"></div>
            <input type="hidden" name="id_${ingRowCount}">
        </div>`;
    container.appendChild(div);
    ingRowCount++;
    updateRowCount();
}
function updateRowCount() {
    const rows = document.querySelectorAll('#ingredient-rows .card-entry');
    document.querySelector('input[name="row_count"]').value = rows.length;
    rows.forEach((r, i) => {
        r.querySelector('strong').textContent = 'Ingredient #' + (i + 1);
        r.querySelectorAll('input, select').forEach(el => {
            el.name = el.name.replace(/_\d+$/, '_' + i);
        });
    });
    ingRowCount = rows.length;
}
</script>
{% endblock %}
```

- [ ] **Step 5: Create `regulated_auth.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Regulated Authorization</h2>
<p class="section-description">Marketing authorization details. Add one row per authorization.</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="{{ rows|length if rows else 1 }}">

    <div id="reg-auth-rows">
        {% set row_list = rows if rows and rows|length > 0 else [{}] %}
        {% for row in row_list %}
        <div class="card-entry mb-3">
            <div class="card-entry-header">
                <strong>Authorization #{{ loop.index }}</strong>
                <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
            </div>
            <div class="row g-2">
                <div class="col-md-3">
                    <label class="form-label">Identifier</label>
                    <input class="form-control" name="identifier_{{ loop.index0 }}" value="{{ row.get('identifier','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Status Date</label>
                    <input type="date" class="form-control" name="statusDate_{{ loop.index0 }}" value="{{ row.get('statusDate','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Type</label>
                    <input class="form-control" name="type_{{ loop.index0 }}" value="{{ row.get('type','') }}" placeholder="e.g. Marketing Authorisation">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Type ID</label>
                    <input class="form-control" name="typeID_{{ loop.index0 }}" value="{{ row.get('typeID','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Reference</label>
                    <select class="form-select" name="reference_{{ loop.index0 }}">
                        <option value="">-- Select --</option>
                        <option value="MedicinalProduct" {% if row.get('reference','') == 'MedicinalProduct' %}selected{% endif %}>Medicinal Product</option>
                        <option value="PackagedProduct" {% if row.get('reference','') == 'PackagedProduct' %}selected{% endif %}>Packaged Product</option>
                    </select>
                </div>
            </div>
            <div class="row g-2 mt-1">
                <div class="col-md-3">
                    <label class="form-label">Region</label>
                    <input class="form-control" name="region_{{ loop.index0 }}" value="{{ row.get('region','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Region ID</label>
                    <input class="form-control" name="regionID_{{ loop.index0 }}" value="{{ row.get('regionID','') }}">
                </div>
                <input type="hidden" name="id_{{ loop.index0 }}" value="{{ row.get('id','') }}">
            </div>
        </div>
        {% endfor %}
    </div>

    <button type="button" class="btn btn-sm btn-outline-secondary mb-3" onclick="addAuthRow()">+ Add Authorization</button>

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>

<script>
let authRowCount = {{ row_list|length if row_list else 1 }};
function addAuthRow() {
    const container = document.getElementById('reg-auth-rows');
    const div = document.createElement('div');
    div.className = 'card-entry mb-3';
    div.innerHTML = `
        <div class="card-entry-header">
            <strong>Authorization #${authRowCount + 1}</strong>
            <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
        </div>
        <div class="row g-2">
            <div class="col-md-3"><label class="form-label">Identifier</label><input class="form-control" name="identifier_${authRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Status Date</label><input type="date" class="form-control" name="statusDate_${authRowCount}"></div>
            <div class="col-md-3"><label class="form-label">Type</label><input class="form-control" name="type_${authRowCount}" placeholder="e.g. Marketing Authorisation"></div>
            <div class="col-md-2"><label class="form-label">Type ID</label><input class="form-control" name="typeID_${authRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Reference</label>
                <select class="form-select" name="reference_${authRowCount}">
                    <option value="">-- Select --</option>
                    <option>MedicinalProduct</option><option>PackagedProduct</option>
                </select></div>
        </div>
        <div class="row g-2 mt-1">
            <div class="col-md-3"><label class="form-label">Region</label><input class="form-control" name="region_${authRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Region ID</label><input class="form-control" name="regionID_${authRowCount}"></div>
            <input type="hidden" name="id_${authRowCount}">
        </div>`;
    container.appendChild(div);
    authRowCount++;
    updateRowCount();
}
function updateRowCount() {
    const rows = document.querySelectorAll('#reg-auth-rows .card-entry');
    document.querySelector('input[name="row_count"]').value = rows.length;
    rows.forEach((r, i) => {
        r.querySelector('strong').textContent = 'Authorization #' + (i + 1);
        r.querySelectorAll('input, select').forEach(el => {
            el.name = el.name.replace(/_\d+$/, '_' + i);
        });
    });
    authRowCount = rows.length;
}
</script>
{% endblock %}
```

- [ ] **Step 6: Create `manufactured_item.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Manufactured Item Definition</h2>
<p class="section-description">Manufactured item details. Exactly one entry.</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="1">
    {% set row = rows[0] if rows and rows|length > 0 else {} %}
    <div class="row g-2 mb-3">
        <div class="col-md-4">
            <label class="form-label">Identifier *</label>
            <input class="form-control" name="identifier_0" value="{{ row.get('identifier','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Dose Form</label>
            <input class="form-control" name="doseForm_0" value="{{ row.get('doseForm','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Dose Form ID</label>
            <input class="form-control" name="doseFormID_0" value="{{ row.get('doseFormID','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Unit Presentation</label>
            <input class="form-control" name="unit_presentation_0" value="{{ row.get('unit_presentation','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Unit Presentation ID</label>
            <input class="form-control" name="unit_presentationID_0" value="{{ row.get('unit_presentationID','') }}">
        </div>
    </div>
    <input type="hidden" name="id_0" value="{{ row.get('id','') }}">

    {% if errors %}{% for e_idx, e_msgs in errors %}{% for msg in e_msgs %}<div class="error-message">{{ msg }}</div>{% endfor %}{% endfor %}{% endif %}

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>
{% endblock %}
```

- [ ] **Step 7: Create `administrable_product.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Administrable Product Definition</h2>
<p class="section-description">Administrable product details. Exactly one entry.</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="1">
    {% set row = rows[0] if rows and rows|length > 0 else {} %}
    <div class="row g-2 mb-3">
        <div class="col-md-4">
            <label class="form-label">Identifier</label>
            <input class="form-control" name="identifier_0" value="{{ row.get('identifier','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Dose Form</label>
            <input class="form-control" name="doseForm_0" value="{{ row.get('doseForm','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Dose Form ID</label>
            <input class="form-control" name="doseFormID_0" value="{{ row.get('doseFormID','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Unit Presentation</label>
            <input class="form-control" name="unit_presentation_0" value="{{ row.get('unit_presentation','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Unit Presentation ID</label>
            <input class="form-control" name="unit_presentationID_0" value="{{ row.get('unit_presentationID','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Route</label>
            <input class="form-control" name="route_0" value="{{ row.get('route','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Route ID</label>
            <input class="form-control" name="routeID_0" value="{{ row.get('routeID','') }}">
        </div>
    </div>
    <input type="hidden" name="id_0" value="{{ row.get('id','') }}">

    {% if errors %}{% for e_idx, e_msgs in errors %}{% for msg in e_msgs %}<div class="error-message">{{ msg }}</div>{% endfor %}{% endfor %}{% endif %}

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>
{% endblock %}
```

- [ ] **Step 8: Create `packaged_product.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Packaged Product Definition</h2>
<p class="section-description">Package details. Add one row per package variant (e.g., 28-count blister, 96-count blister).</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="{{ rows|length if rows else 1 }}">

    <div id="package-rows">
        {% set row_list = rows if rows and rows|length > 0 else [{}] %}
        {% for row in row_list %}
        <div class="card-entry mb-3">
            <div class="card-entry-header">
                <strong>Package #{{ loop.index }}</strong>
                <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
            </div>
            <div class="row g-2">
                <div class="col-md-3">
                    <label class="form-label">Name</label>
                    <input class="form-control" name="name_{{ loop.index0 }}" value="{{ row.get('name','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Identifier</label>
                    <input class="form-control" name="identifier_{{ loop.index0 }}" value="{{ row.get('identifier','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Type</label>
                    <input class="form-control" name="type_{{ loop.index0 }}" value="{{ row.get('type','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Type ID</label>
                    <input class="form-control" name="typeID_{{ loop.index0 }}" value="{{ row.get('typeID','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Status Date</label>
                    <input type="date" class="form-control" name="statusDate_{{ loop.index0 }}" value="{{ row.get('statusDate','') }}">
                </div>
                <div class="col-md-1">
                    <label class="form-label">Quantity</label>
                    <input class="form-control" name="quantity_{{ loop.index0 }}" value="{{ row.get('quantity','') }}" placeholder="28 tablet">
                </div>
            </div>
            <div class="row g-2 mt-1">
                <div class="col-md-2">
                    <label class="form-label">Pkg Quantity</label>
                    <input class="form-control" name="packaging_quantity_{{ loop.index0 }}" value="{{ row.get('packaging_quantity','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Pkg Identifier</label>
                    <input class="form-control" name="packaging_identifier_{{ loop.index0 }}" value="{{ row.get('packaging_identifier','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Packaging Type</label>
                    <input class="form-control" name="Packaging_type_{{ loop.index0 }}" value="{{ row.get('Packaging_type','') }}" placeholder="e.g. Blister">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Type ID</label>
                    <input class="form-control" name="Packaging_typeID_{{ loop.index0 }}" value="{{ row.get('Packaging_typeID','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Material</label>
                    <input class="form-control" name="packaging_material_{{ loop.index0 }}" value="{{ row.get('packaging_material','') }}" placeholder="e.g. Aluminium">
                </div>
                <div class="col-md-1">
                    <label class="form-label">Mat. ID</label>
                    <input class="form-control" name="packaging_materialID_{{ loop.index0 }}" value="{{ row.get('packaging_materialID','') }}">
                </div>
            </div>
            <div class="row g-2 mt-1">
                <div class="col-md-8">
                    <label class="form-label">Description</label>
                    <input class="form-control" name="description_{{ loop.index0 }}" value="{{ row.get('description','') }}">
                </div>
                <div class="col-md-4">
                    <label class="form-label">Copackaged Indicator</label>
                    <select class="form-select" name="copackagedIndicator_{{ loop.index0 }}">
                        <option value="">-- Select --</option>
                        <option value="true" {% if row.get('copackagedIndicator','') == 'true' %}selected{% endif %}>True</option>
                        <option value="false" {% if row.get('copackagedIndicator','') == 'false' %}selected{% endif %}>False</option>
                    </select>
                </div>
                <input type="hidden" name="id_{{ loop.index0 }}" value="{{ row.get('id','') }}">
            </div>
        </div>
        {% endfor %}
    </div>

    <button type="button" class="btn btn-sm btn-outline-secondary mb-3" onclick="addPackageRow()">+ Add Package</button>

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>

<script>
let pkgRowCount = {{ row_list|length if row_list else 1 }};
function addPackageRow() {
    const container = document.getElementById('package-rows');
    const div = document.createElement('div');
    div.className = 'card-entry mb-3';
    div.innerHTML = `
        <div class="card-entry-header">
            <strong>Package #${pkgRowCount + 1}</strong>
            <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
        </div>
        <div class="row g-2">
            <div class="col-md-3"><label class="form-label">Name</label><input class="form-control" name="name_${pkgRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Identifier</label><input class="form-control" name="identifier_${pkgRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Type</label><input class="form-control" name="type_${pkgRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Type ID</label><input class="form-control" name="typeID_${pkgRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Status Date</label><input type="date" class="form-control" name="statusDate_${pkgRowCount}"></div>
            <div class="col-md-1"><label class="form-label">Quantity</label><input class="form-control" name="quantity_${pkgRowCount}" placeholder="28 tablet"></div>
        </div>
        <div class="row g-2 mt-1">
            <div class="col-md-2"><label class="form-label">Pkg Quantity</label><input class="form-control" name="packaging_quantity_${pkgRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Pkg Identifier</label><input class="form-control" name="packaging_identifier_${pkgRowCount}"></div>
            <div class="col-md-3"><label class="form-label">Packaging Type</label><input class="form-control" name="Packaging_type_${pkgRowCount}" placeholder="e.g. Blister"></div>
            <div class="col-md-2"><label class="form-label">Type ID</label><input class="form-control" name="Packaging_typeID_${pkgRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Material</label><input class="form-control" name="packaging_material_${pkgRowCount}" placeholder="e.g. Aluminium"></div>
            <div class="col-md-1"><label class="form-label">Mat. ID</label><input class="form-control" name="packaging_materialID_${pkgRowCount}"></div>
        </div>
        <div class="row g-2 mt-1">
            <div class="col-md-8"><label class="form-label">Description</label><input class="form-control" name="description_${pkgRowCount}"></div>
            <div class="col-md-4"><label class="form-label">Copackaged Indicator</label>
                <select class="form-select" name="copackagedIndicator_${pkgRowCount}">
                    <option value="">-- Select --</option><option value="true">True</option><option value="false">False</option>
                </select></div>
            <input type="hidden" name="id_${pkgRowCount}">
        </div>`;
    container.appendChild(div);
    pkgRowCount++;
    updateRowCount();
}
function updateRowCount() {
    const rows = document.querySelectorAll('#package-rows .card-entry');
    document.querySelector('input[name="row_count"]').value = rows.length;
    rows.forEach((r, i) => {
        r.querySelector('strong').textContent = 'Package #' + (i + 1);
        r.querySelectorAll('input, select').forEach(el => {
            el.name = el.name.replace(/_\d+$/, '_' + i);
        });
    });
    pkgRowCount = rows.length;
}
</script>
{% endblock %}
```

- [ ] **Step 9: Create `clinical_use.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Clinical Use Definition</h2>
<p class="section-description">Indications, contraindications, and interactions. Add one entry per clinical use.</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="{{ rows|length if rows else 1 }}">

    <div id="clinical-rows">
        {% set row_list = rows if rows and rows|length > 0 else [{}] %}
        {% for row in row_list %}
        <div class="card-entry mb-3">
            <div class="card-entry-header">
                <strong>
                    {% set ctype = row.get('type','') or 'New' %}
                    {{ ctype|capitalize }}
                </strong>
                <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
            </div>
            <div class="row g-2">
                <div class="col-md-3">
                    <label class="form-label">Type</label>
                    <select class="form-select" name="type_{{ loop.index0 }}">
                        <option value="">-- Select --</option>
                        <option value="indication" {% if row.get('type','') == 'indication' %}selected{% endif %}>Indication</option>
                        <option value="contraindication" {% if row.get('type','') == 'contraindication' %}selected{% endif %}>Contraindication</option>
                        <option value="interaction" {% if row.get('type','') == 'interaction' %}selected{% endif %}>Interaction</option>
                    </select>
                </div>
                <div class="col-md-4">
                    <label class="form-label">Name</label>
                    <input class="form-control" name="name_{{ loop.index0 }}" value="{{ row.get('name','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Identifier</label>
                    <input class="form-control" name="identifier_{{ loop.index0 }}" value="{{ row.get('identifier','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Identifier System</label>
                    <input class="form-control" name="identifier_system_{{ loop.index0 }}" value="{{ row.get('identifier_system','') }}">
                </div>
            </div>
            <div class="row g-2 mt-1">
                <div class="col-md-3">
                    <label class="form-label">Concept (MedDRA/GSRS code)</label>
                    <input class="form-control" name="conceptID_{{ loop.index0 }}" value="{{ row.get('conceptID','') }}">
                </div>
                <div class="col-md-5">
                    <label class="form-label">Concept Name</label>
                    <input class="form-control" name="concept_{{ loop.index0 }}" value="{{ row.get('concept','') }}">
                </div>
                <input type="hidden" name="id_{{ loop.index0 }}" value="{{ row.get('id','') }}">
            </div>
        </div>
        {% endfor %}
    </div>

    <button type="button" class="btn btn-sm btn-outline-secondary mb-3" onclick="addClinicalRow()">+ Add Clinical Use</button>

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>

<script>
let clinRowCount = {{ row_list|length if row_list else 1 }};
function addClinicalRow() {
    const container = document.getElementById('clinical-rows');
    const div = document.createElement('div');
    div.className = 'card-entry mb-3';
    div.innerHTML = `
        <div class="card-entry-header">
            <strong>New</strong>
            <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
        </div>
        <div class="row g-2">
            <div class="col-md-3"><label class="form-label">Type</label>
                <select class="form-select" name="type_${clinRowCount}" onchange="this.closest('.card-entry').querySelector('strong').textContent = this.value.charAt(0).toUpperCase() + this.value.slice(1)">
                    <option value="">-- Select --</option>
                    <option>indication</option><option>contraindication</option><option>interaction</option>
                </select></div>
            <div class="col-md-4"><label class="form-label">Name</label><input class="form-control" name="name_${clinRowCount}"></div>
            <div class="col-md-3"><label class="form-label">Identifier</label><input class="form-control" name="identifier_${clinRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Identifier System</label><input class="form-control" name="identifier_system_${clinRowCount}"></div>
        </div>
        <div class="row g-2 mt-1">
            <div class="col-md-3"><label class="form-label">Concept (MedDRA/GSRS code)</label><input class="form-control" name="conceptID_${clinRowCount}"></div>
            <div class="col-md-5"><label class="form-label">Concept Name</label><input class="form-control" name="concept_${clinRowCount}"></div>
            <input type="hidden" name="id_${clinRowCount}">
        </div>`;
    container.appendChild(div);
    clinRowCount++;
    updateRowCount();
}
function updateRowCount() {
    const rows = document.querySelectorAll('#clinical-rows .card-entry');
    document.querySelector('input[name="row_count"]').value = rows.length;
    rows.forEach((r, i) => {
        const sel = r.querySelector('select');
        const ctype = sel ? sel.value : '';
        r.querySelector('strong').textContent = ctype ? ctype.charAt(0).toUpperCase() + ctype.slice(1) : 'New';
        r.querySelectorAll('input, select').forEach(el => {
            el.name = el.name.replace(/_\d+$/, '_' + i);
        });
    });
    clinRowCount = rows.length;
}
</script>
{% endblock %}
```

- [ ] **Step 10: Create `composition.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Composition (ePI Document)</h2>
<p class="section-description">ePI document composition with leaflet sections. Add one per language.</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="{{ rows|length if rows else 1 }}">

    <div id="composition-rows">
        {% set row_list = rows if rows and rows|length > 0 else [{}] %}
        {% for row in row_list %}
        <div class="card-entry mb-3">
            <div class="card-entry-header">
                <strong>Composition #{{ loop.index }} ({{ row.get('language','en') }})</strong>
                <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
            </div>
            <div class="row g-2">
                <div class="col-md-4">
                    <label class="form-label">Name</label>
                    <input class="form-control" name="name_{{ loop.index0 }}" value="{{ row.get('name','') }}">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Language</label>
                    <input class="form-control" name="language_{{ loop.index0 }}" value="{{ row.get('language','en') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Date</label>
                    <input type="date" class="form-control" name="date_{{ loop.index0 }}" value="{{ row.get('date','') }}">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Identifier System</label>
                    <input class="form-control" name="identifier_system_{{ loop.index0 }}" value="{{ row.get('identifier_system','') }}">
                </div>
                <input type="hidden" name="identifier_{{ loop.index0 }}" value="{{ row.get('identifier','') }}">
            </div>
            <hr>
            <div class="row g-2">
                <div class="col-md-6">
                    <label class="form-label">Package Leaflet (HTML)</label>
                    <textarea class="form-control" name="package_leaflet_{{ loop.index0 }}" rows="3">{{ row.get('package_leaflet','') }}</textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label">Information for the User</label>
                    <textarea class="form-control" name="information_user_{{ loop.index0 }}" rows="3">{{ row.get('information_user','') }}</textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label">What is in this leaflet</label>
                    <textarea class="form-control" name="what_in_leaflet_{{ loop.index0 }}" rows="3">{{ row.get('what_in_leaflet','') }}</textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label">What the product is</label>
                    <textarea class="form-control" name="what_product_is_{{ loop.index0 }}" rows="3">{{ row.get('what_product_is','') }}</textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label">Before you take</label>
                    <textarea class="form-control" name="before_take_{{ loop.index0 }}" rows="3">{{ row.get('before_take','') }}</textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label">How to take</label>
                    <textarea class="form-control" name="how_to_take_{{ loop.index0 }}" rows="3">{{ row.get('how_to_take','') }}</textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label">Side Effects</label>
                    <textarea class="form-control" name="side_effects_{{ loop.index0 }}" rows="3">{{ row.get('side_effects','') }}</textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label">How to Store</label>
                    <textarea class="form-control" name="how_to_store_{{ loop.index0 }}" rows="3">{{ row.get('how_to_store','') }}</textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label">Other Information</label>
                    <textarea class="form-control" name="other_info_{{ loop.index0 }}" rows="3">{{ row.get('other_info','') }}</textarea>
                </div>
                <input type="hidden" name="id_{{ loop.index0 }}" value="{{ row.get('id','') }}">
            </div>
        </div>
        {% endfor %}
    </div>

    <button type="button" class="btn btn-sm btn-outline-secondary mb-3" onclick="addCompRow()">+ Add Composition</button>

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>

<script>
let compRowCount = {{ row_list|length if row_list else 1 }};
function addCompRow() {
    const container = document.getElementById('composition-rows');
    const div = document.createElement('div');
    div.className = 'card-entry mb-3';
    div.innerHTML = `
        <div class="card-entry-header">
            <strong>Composition #${compRowCount + 1} (en)</strong>
            <button type="button" class="btn btn-sm btn-outline-danger" onclick="this.closest('.card-entry').remove(); updateRowCount();">Remove</button>
        </div>
        <div class="row g-2">
            <div class="col-md-4"><label class="form-label">Name</label><input class="form-control" name="name_${compRowCount}"></div>
            <div class="col-md-2"><label class="form-label">Language</label><input class="form-control" name="language_${compRowCount}" value="en"></div>
            <div class="col-md-3"><label class="form-label">Date</label><input type="date" class="form-control" name="date_${compRowCount}"></div>
            <div class="col-md-3"><label class="form-label">Identifier System</label><input class="form-control" name="identifier_system_${compRowCount}"></div>
            <input type="hidden" name="identifier_${compRowCount}">
        </div>
        <hr>
        <div class="row g-2">
            <div class="col-md-6"><label class="form-label">Package Leaflet (HTML)</label><textarea class="form-control" name="package_leaflet_${compRowCount}" rows="3"></textarea></div>
            <div class="col-md-6"><label class="form-label">Information for the User</label><textarea class="form-control" name="information_user_${compRowCount}" rows="3"></textarea></div>
            <div class="col-md-6"><label class="form-label">What is in this leaflet</label><textarea class="form-control" name="what_in_leaflet_${compRowCount}" rows="3"></textarea></div>
            <div class="col-md-6"><label class="form-label">What the product is</label><textarea class="form-control" name="what_product_is_${compRowCount}" rows="3"></textarea></div>
            <div class="col-md-6"><label class="form-label">Before you take</label><textarea class="form-control" name="before_take_${compRowCount}" rows="3"></textarea></div>
            <div class="col-md-6"><label class="form-label">How to take</label><textarea class="form-control" name="how_to_take_${compRowCount}" rows="3"></textarea></div>
            <div class="col-md-6"><label class="form-label">Side Effects</label><textarea class="form-control" name="side_effects_${compRowCount}" rows="3"></textarea></div>
            <div class="col-md-6"><label class="form-label">How to Store</label><textarea class="form-control" name="how_to_store_${compRowCount}" rows="3"></textarea></div>
            <div class="col-md-6"><label class="form-label">Other Information</label><textarea class="form-control" name="other_info_${compRowCount}" rows="3"></textarea></div>
            <input type="hidden" name="id_${compRowCount}">
        </div>`;
    container.appendChild(div);
    compRowCount++;
    updateRowCount();
}
function updateRowCount() {
    const rows = document.querySelectorAll('#composition-rows .card-entry');
    document.querySelector('input[name="row_count"]').value = rows.length;
    rows.forEach((r, i) => {
        r.querySelector('strong').textContent = 'Composition #' + (i + 1);
        r.querySelectorAll('input, textarea').forEach(el => {
            el.name = el.name.replace(/_\d+$/, '_' + i);
        });
    });
    compRowCount = rows.length;
}
</script>
{% endblock %}
```

- [ ] **Step 11: Create `bundle.html`**

```html
{% extends "wizard/base.html" %}
{% block content %}
<h2>Bundle</h2>
<p class="section-description">FHIR Bundle metadata. Final step before generation.</p>

<form method="post"
      hx-post="{{ url_for('gh_epi_creator.wizard_submit', step=step) }}"
      hx-target="body" hx-swap="outerHTML">
    <input type="hidden" name="row_count" value="1">
    {% set row = rows[0] if rows and rows|length > 0 else {} %}
    <div class="row g-2 mb-3">
        <div class="col-md-4">
            <label class="form-label">Language *</label>
            <input class="form-control" name="language_0" value="{{ row.get('language','en') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Identifier System</label>
            <input class="form-control" name="identifier_system_0" value="{{ row.get('identifier_system','') }}">
        </div>
        <div class="col-md-4">
            <label class="form-label">Identifier Value</label>
            <input class="form-control" name="identifier_value_0" value="{{ row.get('identifier_value','') }}">
        </div>
    </div>
    <input type="hidden" name="id_0" value="{{ row.get('id','') }}">

    <div class="alert alert-info mt-3">
        <strong>Ready to generate!</strong> Click "Save &amp; Next" to save the Bundle data, then click "Generate FHIR Package" in the sidebar.
    </div>

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ url_for('gh_epi_creator.wizard_step', step=steps[steps.index(step)-1]) }}" class="btn btn-outline-secondary btn-nav">Back</a>
        <button type="submit" class="btn btn-primary btn-nav">Save &amp; Next</button>
    </div>
</form>
{% endblock %}
```

- [ ] **Step 12: Commit**

```bash
git add epi_creator/templates/wizard/
git commit -m "feat: add all 11 wizard form templates"
```

---
### Task 8: Update Landing Page

**Files:**
- Modify: `epi_creator/templates/index.html`

- [ ] **Step 1: Replace `index.html`**

Replace entire file with wizard entry point:

```html
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/main.css') }}">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-b5kHyXgcpbZJO/tY9Ul7kGkf1S0CWuKcCD38l8YkeH8z8QjE0GmW1gYU5S9FOnJ0"
            crossorigin="anonymous"></script>
    <style>
        .container { margin-top: 60px; max-width: 700px; }
        .hero { text-align: center; padding: 40px 20px; }
    </style>
</head>
<body>
<div class="container">
    <div class="hero">
        <h1>ePI Creator</h1>
        <p class="lead mt-3">Create FHIR ePI resources using a guided step-by-step wizard. No Excel required.</p>
        <p class="text-muted">Fill in the forms across 11 steps to generate a complete FHIR package with FSH and JSON resources.</p>

        <form method="post" action="{{ url_for('gh_epi_creator.wizard_new') }}" class="mt-4">
            <button type="submit" class="btn btn-primary btn-lg px-5">Start Wizard</button>
        </form>

        <div class="mt-4">
            <p>
                <a href="{{ url_for('gh_epi_creator.faq') }}">FAQ &amp; Resource Relationships</a> |
                <a href="http://build.fhir.org/ig/HL7/emedicinal-product-info/artifacts.html">FHIR Profiles</a> |
                <a href="http://build.fhir.org/ig/hl7-eu/gravitate-health/">Gravitate Health IG</a>
            </p>
        </div>
    </div>
</div>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add epi_creator/templates/index.html
git commit -m "feat: replace upload landing page with wizard start page"
```

---
### Task 9: Fix Template Routing (step template dispatch)

**Files:**
- Modify: `epi_creator/views.py`

The current `wizard_step` route renders `base.html` but each step needs its own form template included. Let's fix the dispatch.

- [ ] **Step 1: Update `wizard_step()` GET to render step-specific template**

In `views.py`, replace the `wizard_step()` function with one that renders the correct form template:

```python
@gh_epi_creator.route("/wizard/<step>", methods=["GET"])
def wizard_step(step):
    if "data" not in session:
        session["data"] = {}
    data = session["data"]
    sheet_name = SHEET_NAMES.get(step)
    rows = data.get(sheet_name, [])
    is_single = sheet_name in ("MedicinalProductDefinition", "ManufacturedItemDefinition", "AdministrableProductDefinition")

    step_template = step.replace("-", "_") + ".html"
    return render_template(
        "wizard/" + step_template,
        step=step,
        sheet_name=sheet_name,
        rows=rows,
        steps=STEPS,
        current_step=step,
        step_title=sheet_name.replace("Definition", "").replace("Definition", ""),
        is_single=is_single,
    )
```

Similarly update `wizard_submit()` to render the correct redirect template:

```python
@gh_epi_creator.route("/wizard/<step>", methods=["POST"])
def wizard_submit(step):
    if "data" not in session:
        session["data"] = {}
    sheet_name = SHEET_NAMES.get(step)

    form_data = request.form.to_dict(flat=False)
    rows = parse_form_rows(form_data, sheet_name)

    validation_errors = validate_sheet_data(rows, sheet_name)

    if validation_errors:
        is_single = sheet_name in ("MedicinalProductDefinition", "ManufacturedItemDefinition", "AdministrableProductDefinition")
        step_template = step.replace("-", "_") + ".html"
        return render_template(
            "wizard/" + step_template,
            step=step,
            sheet_name=sheet_name,
            rows=rows,
            steps=STEPS,
            current_step=step,
            step_title=sheet_name.replace("Definition", ""),
            errors=validation_errors,
            is_single=is_single,
        )

    session["data"][sheet_name] = rows
    session.modified = True

    current_idx = STEPS.index(step)
    if current_idx < len(STEPS) - 1:
        next_step = STEPS[current_idx + 1]
        next_sheet = SHEET_NAMES[next_step]
        next_template = next_step.replace("-", "_") + ".html"
        is_single = next_sheet in ("MedicinalProductDefinition", "ManufacturedItemDefinition", "AdministrableProductDefinition")
        return render_template(
            "wizard/" + next_template,
            step=next_step,
            sheet_name=next_sheet,
            rows=session["data"].get(next_sheet, []),
            steps=STEPS,
            current_step=next_step,
            step_title=next_sheet.replace("Definition", ""),
            is_single=is_single,
        )
    else:
        return render_template(
            "wizard/bundle.html",
            step="bundle",
            sheet_name="Bundle",
            rows=session["data"].get("Bundle", []),
            steps=STEPS,
            current_step="bundle",
            step_title="Bundle",
            is_single=False,
        )
```

- [ ] **Step 2: Commit**

```bash
git add epi_creator/views.py
git commit -m "fix: wizard routes render step-specific templates"
```

---
### Task 10: Integration — Smoke Test

**Files:**
- None (verification only)

- [ ] **Step 1: Start the Flask app**

```bash
cd /Users/joaoalmeida/Desktop/hl7Europe/gravitate/epi-creator-webapp && python3 run.py &
sleep 3
```

- [ ] **Step 2: Test landing page loads**

```bash
curl -s http://localhost:5005/gh-epi-creator/ | head -20
```
Expected: HTML page with "ePI Creator" and "Start Wizard" button.

- [ ] **Step 3: Test wizard new creates session**

```bash
curl -s -c /tmp/cookies.txt -X POST http://localhost:5005/gh-epi-creator/wizard/new -w "%{redirect_url}" -o /dev/null
```
Expected: Redirect to `/gh-epi-creator/wizard/organization`.

- [ ] **Step 4: Test first step loads**

```bash
curl -s -b /tmp/cookies.txt http://localhost:5005/gh-epi-creator/wizard/organization | head -30
```
Expected: Organization form page loads.

- [ ] **Step 5: Test lookup API**

```bash
curl -s http://localhost:5005/gh-epi-creator/api/lookup/orgTypes | python3 -m json.tool | head -20
```

- [ ] **Step 6: Kill test server**

```bash
kill %1 2>/dev/null || true
```

- [ ] **Step 7: Commit (if any fixes made)**

```bash
git status
```

---
### Task 11: Cleanup — Remove Old Excel Code from views.py

**Files:**
- Modify: `epi_creator/views.py`

- [ ] **Step 1: Remove the old `upload_file()` and `download_file()` functions**

In `views.py`, remove the old `upload_file` function (lines 48-69 from original) and the old `download_file` (lines 36-45 from original). Also remove unused imports.

Remove these lines from the imports:
```python
from werkzeug.utils import secure_filename
```
And:
```python
from epi_creator.functions import process_file
```

Keep only what the wizard uses:
```python
import os

from flask import (
    Blueprint,
    jsonify,
    redirect,
    render_template,
    request,
    send_from_directory,
    session,
    url_for,
)
from flask import current_app as app

from epi_creator.functions import generate_from_session
from epi_creator.validator import validate_row, validate_sheet_data, validate_pre_generation
from epi_creator.lookup import get_lookup, get_category_id
```

- [ ] **Step 2: Commit**

```bash
git add epi_creator/views.py
git commit -m "chore: remove old Excel upload/download routes"
```

---
### Task 12: Self-Review Pass

**Files:**
- Review: all changed files

- [ ] **Step 1: Verify all route names match**

```bash
cd /Users/joaoalmeida/Desktop/hl7Europe/gravitate/epi-creator-webapp && grep -r "url_for" epi_creator/ --include="*.py" --include="*.html" | sort
```

- [ ] **Step 2: Verify no references to removed functions**

```bash
cd /Users/joaoalmeida/Desktop/hl7Europe/gravitate/epi-creator-webapp && grep -r "process_file\|upload_file\|acmeDrug\.xlsx\|read_excel" epi_creator/ --include="*.py" | grep -v "#" | grep -v "import"
```
Expected: No results (or only references to `acmeDrug_1.xlsx` in `lookup.py`).

- [ ] **Step 3: Start server and do a full manual flow test**

```bash
cd /Users/joaoalmeida/Desktop/hl7Europe/gravitate/epi-creator-webapp && python3 run.py
```
Open http://localhost:5005/gh-epi-creator/ in a browser. Click "Start Wizard", walk through all 11 steps filling in data, click "Generate FHIR Package".

- [ ] **Step 4: Fix any issues found and commit**

---
